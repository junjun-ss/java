# 15 String Parsing

문자열 분리, 문자 순회, 숫자 변환, `StringBuilder` 문제가 나오면 이 폴더를 봅니다.

| 파일 | 용도 |
| --- | --- |
| `StringParsingExample.java` | split, charAt, Character, parseInt, StringBuilder |

가장 먼저 볼 파일: `StringParsingExample.java`
